# Yarn PnP Files
.pnp.*
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.yarn/patches
.yarn/patches/*

# Optional if you use Yarn's Zero-Installs feature
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node Modules (if using `nodeLinker: node-modules`)
node_modules/

# Distribution files
dist
dist-ssr

# Local environment files
*.local

# Environment files
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ignore lock files (optional if using only Yarn PnP)
yarn.lock
package-lock.json
pnpm-lock.yaml

# Ignore Yarn's local state files
.yarn/install-state.json
.yarn/global
.yarn

# Environment files
.env
